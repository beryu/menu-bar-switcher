# Repository guidance

## 目的

このリポジトリは、ノッチのある近年の MacBook でメニューバー項目がノッチに隠れ、操作できなくなる問題を解決する macOS アプリのためのものです。

目指す体験は次のとおりです。

1. アプリがメニューバーに「⋯」アイコンを表示する。
2. 普段は対象のメニューバー項目をその中に収め、メニューバー上の占有幅を減らす。
3. 「⋯」をクリックすると、メニューバーの下に擬似的なメニューバー UI を表示する。
4. その UI から隠した項目を選択し、可能な限り通常の macOS メニューバーと同じように操作できる。

## 実装方針

- 操作対象は、このアプリ自身の項目だけでなく、ほかのアプリが表示するメニューバー項目も想定する。
- 擬似的な UI はアイコンを並べるだけで終わらせず、クリック後のメニュー表示やアプリへの操作の引き渡しまで検討する。
- macOS の公開 API と必要な権限を調べ、項目の検出、表示、移動、操作について実現可能な範囲を確認してから実装方法を決める。実現できない操作は、できるように見せかけない。
- アクセシビリティ権限などが必要な場合は、要求する理由と有効化の手順を利用者に明示する。取得する情報と権限は機能に必要な範囲に限る。
- ノッチの有無、画面サイズ、外部ディスプレイ、メニューバーの自動非表示、複数ディスプレイでの動作を考慮する。
- 既存の Xcode プロジェクトと Swift コードを基点に、小さく検証可能な単位で実装する。

## 完了の判断

- 「⋯」から項目を見つけて選択できることを、実機の macOS で確認する。
- ネイティブのメニューバーと同等に操作できない項目や条件があれば、その制約と代替操作を記録する。
- UI の見た目だけでなく、キーボード操作、VoiceOver、権限を拒否した場合の案内も確認する。
